<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to "Iterable Events", root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <% if user_signed_in? %>
            <%= link_to "Events", events_path, class: "btn" %>
          <% end %>
        </li>
      </ul>
      <% if user_signed_in? %>
        <form class="d-flex" role="search">
          <%= form_tag search_path, method: :get do %>
            <%= search_field_tag :search, params[:search], class: "form-control me-2" %>
            <%= button_tag "Search", name: nil, class: "btn btn-outline-success" %>&nbsp&nbsp
          <% end %>
        </form>

        <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-outline-danger" %>
      <% else %>
        <%= link_to "Sign in", new_user_session_path, class:"btn btn-outline-danger" %>
      <% end %>
    </div>
  </div>
</nav>
